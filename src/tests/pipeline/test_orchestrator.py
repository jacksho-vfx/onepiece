"""Placeholder coverage for the Trafalgar pipeline orchestrator."""

import pytest

pytestmark = pytest.mark.skip(reason="Orchestrator behaviour tests will arrive with upcoming features")


def test_placeholder() -> None:
    """Keep pytest collection happy until real orchestrator tests land."""

    assert True
