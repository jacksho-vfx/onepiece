"""Seeded tests for future pipeline configuration helpers."""

import pytest

pytestmark = pytest.mark.skip(reason="Configuration helpers will be tested in upcoming changes")


def test_placeholder() -> None:
    """Placeholder assertion so pytest recognises this module."""

    assert True
