-- Bake render layer settings and set defaults for OnePiece submissions.

messageBox "Render layer placeholder: configure lighting overrides and activate AOV presets." title:"OnePiece: Render Layers"
