# OnePiece Studio Desktop build resources

This directory holds platform-specific assets consumed by `electron-builder`,
such as icons, installer backgrounds, and code-signing entitlements. Replace the
placeholder contents with real artwork when preparing production installers.
